{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Farmbot main",
            "type": "python",
            "request": "launch",
            "module": "src.main",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}